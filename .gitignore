# Independent repositories, cloned side by side — see docs/SETUP-AND-WIRING.md
/flutter/
/Android_Ble_SDK/

node_modules/
.wrangler/
*.log
.DS_Store

# Secrets guardrail — see docs/SETUP-AND-WIRING.md Step 2.7.
# Encrypted dotenvx files are the exception: they hold no plaintext secrets.
.env.keys
*serviceAccount*.json
*service-account*.json
*.jks
*.keystore
key.properties
.dev.vars
.env
.env.*
!.env.example
!.env.dev
!.env.staging
!.env.production

# Local agent configuration, approvals, endpoints, and session logs.
CLAUDE.local.md
.claude/settings.local.json
.mcp.json
.codex-log/

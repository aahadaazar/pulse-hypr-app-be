# Used by `npm run build` (Vite production mode) and the CI deploy pipeline.
# Must match the Worker URL `wrangler deploy --env production` publishes
# (backend/wrangler.toml [env.production]) -- see .github/workflows/deploy.yml.
VITE_API_BASE_URL=https://pulse-hypr-api-production.aahadaazar.workers.dev/v1
